import { Meta } from '@storybook/blocks'
import { DocsTable } from '../../src/components/DocsTable'
import { TokenMappingTable } from '../../src/components/TokenMappingTable'

<Meta title="Migrations/Color v2/Token Mapping" />

# Token Mapping

Legacy Verity / Command CSS variables mapped to Color v2 semantic tokens. Search the full semantic inventory in [Reference / Semantic Tokens](/docs/reference-semantic-tokens--docs).

<TokenMappingTable />

## Dialect notes

<DocsTable
  headers={['Legacy prefix', 'Origin', 'Migration approach']}
  codeColumns={[0]}
  rows={[
    ['--color-*', 'Verity core', 'Direct mapping to --text/*, --background/*'],
    ['--bg-*', 'Early Command', 'Map to --background/*'],
    ['--vc-*', 'Verity charts', 'Map chart-specific vars to --text/secondary, --chart/*'],
    ['--vs-*', 'Verity shared components', 'Map to component semantic groups'],
    ['--vd-*', 'Verity docs/internal', 'Map to --link/*, --text/*'],
  ]}
/>

After mapping, validate with your product-line example in [Teams Overview](/docs/teams-overview--docs).
