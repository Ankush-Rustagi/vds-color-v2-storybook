import { Meta } from '@storybook/blocks'
import { ColorPrimitivesReference } from '../../src/components/SimpleTokenReference'

<Meta title="Reference/Color Primitives" />

# Color Primitives

Raw palette scales from the Figma Collection. Prefer semantic tokens in component code; use primitives when building new semantic mappings with Design System.

<ColorPrimitivesReference />
