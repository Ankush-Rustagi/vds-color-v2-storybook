import { Meta } from '@storybook/blocks'
import { SizeTokensReference } from '../../src/components/SimpleTokenReference'

<Meta title="Reference/Size Tokens" parameters={{ docs: { toc: false } }} />

# Size Tokens

Icon, radius, space, and stroke tokens from Figma Collection (79 tokens).

<SizeTokensReference />
